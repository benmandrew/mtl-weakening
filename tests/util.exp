# Set terminal dimensions only for spawned processes
set env(COLUMNS) 160
set env(LINES) 40

proc expect_exact {text} {
	expect {
		-exact $text {}
		timeout {
			puts "Expected: '$text'"
			exit 1
		}
		eof {
			puts "Program ended too early (wanted '$text')"
			exit 1
		}
	}
}
