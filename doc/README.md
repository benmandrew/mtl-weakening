# Artefacts

Full proofs of correctness and optimality are located in [proofs.pdf](proofs.pdf).

The table containing interval-weakenable requirements in FRET case studies can be produced by running `make requirements` in this directory.

The paper itself can be build by running `make tex`.
