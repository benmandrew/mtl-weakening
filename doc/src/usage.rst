Usage
=====

Build the documentation from the repository root:

.. code-block:: bash

   make docs

Clean the documentation build output:

.. code-block:: bash

   make docs-clean

The generated HTML is written to ``doc/build/html``.
